# ============================================================
# Industrial Energy Efficiency Copilot v2 — Backend Dependencies
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
sse-starlette>=2.1.0

# --- PDF parsing ---
PyMuPDF==1.24.14
pdfplumber==0.11.4

# --- OCR (v2: REQUIRED — both PDFs are fully image-scanned) ---
pytesseract>=0.3.13
Pillow>=10.0.0
opencv-python-headless>=4.10.0
# System install needed: sudo apt-get install tesseract-ocr tesseract-ocr-eng

# --- LangGraph orchestration (v2) ---
langgraph>=0.2.0,<0.3.0
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0

# --- Caching (v2) ---
diskcache>=5.6.3

# --- Embeddings & ML ---
sentence-transformers==3.3.1

# --- Vector store ---
chromadb==0.5.23

# --- Sparse retrieval ---
rank-bm25==0.2.2

# --- LLM clients (Mistral API via httpx) ---
# httpx is already included below

# --- Data utilities ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
numpy>=1.26.0,<2
tqdm==4.67.1
rich==13.9.4

# --- Text processing ---
nltk==3.9.1
tiktoken==0.8.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx>=0.28.1

# --- Async ---
anyio==4.8.0

# --- Serialization ---
msgpack==1.1.0
